_schema-version: "3.2"
ID: tsystems-invoices2
description: Test fior free style app
version: 0.0.1
modules:
- name: tsystems-invoices2-destination-content
  type: com.sap.application.content
  requires:
  - name: tsystems-invoices2-destination-service
    parameters:
      content-target: true
  - name: tsystems-invoices2-repo-host
    parameters:
      service-key:
        name: tsystems-invoices2-repo-host-key
  - name: tsystems-invoices2-uaa
    parameters:
      service-key:
        name: tsystems-invoices2-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: tsystems-invoices2_html_repo_host
          ServiceInstanceName: tsystems-invoices2-html5-srv
          ServiceKeyName: tsystems-invoices2-repo-host-key
          sap.cloud.service: tsystems-invoices2
        - Authentication: OAuth2UserTokenExchange
          Name: tsystems-invoices2_uaa
          ServiceInstanceName: tsystems-invoices2-xsuaa-srv
          ServiceKeyName: tsystems-invoices2-uaa-key
          sap.cloud.service: tsystems-invoices2
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: tsystems-invoices2-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tsystems-invoices2-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tsystemsinvoices2.zip
      name: tsystemsinvoices2
      target-path: resources/
- name: tsystemsinvoices2
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tsystems-invoices2-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: tsystems-invoices2-destination-service
    service-plan: lite
- name: tsystems-invoices2-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: tsystems-invoices2-xsuaa-srv
    service-plan: application
- name: tsystems-invoices2-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tsystems-invoices2-html5-srv
    service-plan: app-host
# UI Theme Designer
- name: tsystems-invoices2-theming
  parameters:
      service: theming
      service-plan: standard
  type: org.cloudfoundry.managed-service
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
