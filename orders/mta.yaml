_schema-version: "3.2"
ID: tsystems-orders
description: A Fiori application consuming Northwind ODATA
version: 0.0.1
modules:
- name: tsystems-orders-destination-content
  type: com.sap.application.content
  requires:
  - name: tsystems-orders-destination-service
    parameters:
      content-target: true
  - name: tsystems-orders-repo-host
    parameters:
      service-key:
        name: tsystems-orders-repo-host-key
  - name: tsystems-orders-uaa
    parameters:
      service-key:
        name: tsystems-orders-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: tsystems-orders_html_repo_host
          ServiceInstanceName: tsystems-orders-html5-srv
          ServiceKeyName: tsystems-orders-repo-host-key
          sap.cloud.service: tsystems-orders
        - Authentication: OAuth2UserTokenExchange
          Name: tsystems-orders_uaa
          ServiceInstanceName: tsystems-orders-xsuaa-srv
          ServiceKeyName: tsystems-orders-uaa-key
          sap.cloud.service: tsystems-orders
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: tsystems-orders-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tsystems-orders-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tsystemsorders.zip
      name: tsystemsorders
      target-path: resources/
- name: tsystemsorders
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tsystems-orders-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: tsystems-orders-destination-service
    service-plan: lite
- name: tsystems-orders-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: tsystems-orders-xsuaa-srv
    service-plan: application
- name: tsystems-orders-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tsystems-orders-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
